# carflip.scoring — pure Python deal evaluation
# ARCHITECTURAL CONSTRAINT: this package must NEVER import from carflip.llm
# Enforced by: grep -r "from carflip.llm" carflip/scoring/ should return nothing
