# autoflipr.scoring — pure Python deal evaluation
# ARCHITECTURAL CONSTRAINT: this package must NEVER import from autoflipr.llm
# Enforced by: grep -r "from autoflipr.llm" autoflipr/scoring/ should return nothing
